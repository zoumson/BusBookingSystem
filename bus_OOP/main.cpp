//  main.cpp
//  Created by ZOUMA Adama on 10/31/19.
//  Copyright © 2019 ZOUMA Adama. All rights reserved.

#include <iostream>
#include <string.h>
#include "Bus.h"
#include "Function.h"
#include "Menu.h"
#include <vector>
using namespace std;


int main(int argc, const char * argv[]) {

     run(5);



/*
     vector < Bus *> Buses;

     unsigned int bus_active = 0;
     //design(start, 2, &b);

     design(bus_active, 5, Buses);

     if(Buses.at(0)->isFull())cout << Buses.at(0)->getDriverName();

     cout <<endl;

     //cout << Buses.at(0)->getPassenger(Buses.at(0)->getSize());

     for(int i = 1; i <= Buses.at(0)->getSize(); i++)
     {
       cout << Buses.at(0)->getPassenger(i) <<endl;
     }

*/

     //cout << Buses.at(0)->getDriverName();


    // design(unsigned int &, const unsigned int, vector< Bus *> &);

     //vector < Bus *> Buses;
     //for(int i = 0; i < total_bus; i++)Buses.push_back(Bus());
     //unsigned int bus_active = 0;

     //cout <<b[0]->getSize;
     //vector < Bus> b;

     //b.reserve(2);
    // unsigned int i = 10;
    //Bus d("a", "a", "a", "a", "a", "a", 1);

    //cout <<"\n" << d.getPassenger(1)<<"\n";
    //if(d.isFull())cout << "\nFull\n";

    // Bus c;

     //c = d;
     //c.setSize(10);
     //cout << c.getSize() <<"\n";



     //cout << c.getBusNum();

     /*
     cout << "\nEnter bus no: ";

     string num;

     cin >> num;

     //b[bus_active].setBusNum(num);

     cout << "\nEnter bus size: ";

     int siz;

     cin >> siz;

     //b[bus_active].reSize(siz);

     cout << "\nEnter Driver's name: ";

     string nam;

     cin >> nam;

     //b[bus_active].setDriverName(nam);

     cout << "\nArrival time: ";

     string arr;

     cin >> arr;

     //b[bus_active].setArrivalTime(arr);

     cout << "\nDeparture: ";

     string dep;

     cin >> dep;

     //b[bus_active].setDepartTime(dep);

     cout<<"\nFrom: \t\t\t";

     string from;

     cin >> from;

     //b[bus_active + 1].setProvenance(from);

     cout << "\nTo: \t\t\t";

     string to;

     cin >> to;
*/
     //b[bus_active].setDestination(to);

     //Bus *result = new Bus(num, nam, arr, dep, from, to, siz);


     //Bus *result = new Bus(num, nam, arr, dep, from, to, siz);
     //b.push_back(new Bus(num, nam, arr, dep, from, to, siz));

    return 0;
}
